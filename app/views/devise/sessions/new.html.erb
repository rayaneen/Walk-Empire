
<body>

<style>

body{
  color:white;
  background: url(https://images.unsplash.com/photo-1646087816237-e4c4d4cd69d0?ixlib=rb-1.2.1&ixid=MnwxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8&auto=format&fit=crop&w=2070&q=80);
  background-position: 0 -169px;
}

</style>

<h2 style="text-align:center;">Welcome ! 🎉</h2>

<div id="figma">
  <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
    <div class="form-inputs" id="loginwe">
      <%= f.input :email,
                  required: false,
                  autofocus: true,
                  input_html: { autocomplete: "email", class:'emailwe'} %>
      <%= f.input :password,
                  required: false,
                  input_html: { autocomplete: "current-password", class:'passwe' } %>
    </div>

    <div class="form-actions">
      <%= f.button :submit, "Log in 􀄫", class:'btnlogin' %>
      <div class="d-flex justify-content-between">
        <%= f.input :remember_me, as: :boolean if devise_mapping.rememberable? %>
        <%- if devise_mapping.recoverable? && controller_name != 'passwords' && controller_name != 'registrations' %>
          <%= link_to "  Forgot Password?", new_password_path(resource_name), style: 'text-decoration: none;color: white;', class: 'mb-3' %><br />
        <% end %>
      </div>
    </div>
  <% end %>
<%= render "devise/shared/links" %>
</div>
</body>
